name: Colab Automation Every 17 Minutes

on:
  schedule:
    # Runs every 17 minutes during work hours (8 AM - 6 PM UTC)
    - cron: '0,17,34,51 8-18 * * 1-5'  # Mon-Fri, every 17 min, 8-6 PM
  workflow_dispatch:  # Allows manual triggering

jobs:
  run-automation:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install Chrome
      run: |
        sudo apt-get update
        sudo apt-get install -y google-chrome-stable
        
    - name: Install Python dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Download state file from previous run
      uses: actions/download-artifact@v4
      with:
        name: automation-state
        path: .
      continue-on-error: true
      
    - name: Check if state file was downloaded
      run: |
        if [ -f automation_state.json ]; then
          echo "✅ Previous state file found and downloaded"
          cat automation_state.json
        else
          echo "📝 No previous state file found - this is normal for first run"
        fi
    
    - name: Create default state if none exists
      run: |
        if [ ! -f automation_state.json ]; then
          echo '{"current_project": null, "current_sample_index": 0, "processed_samples": [], "failed_samples": [], "completed_projects": [], "last_run_time": null, "total_samples_processed": 0}' > automation_state.json
          echo "🆕 Created default state file"
        fi
        
    - name: Run automation script with real UK timing
      run: python automation_script.py
      
    - name: Upload updated state file
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: automation-state
        path: automation_state.json
        
    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ Automation failed at $(date)"
